---
description: Magic Book 前端代码开发规范
alwaysApply: true
---

# 前端代码开发规范

## 基础原则

- 遵循《代码整洁之道》的 coding 方式
- 使用函数式编程和 React Hooks
- 设计原则: 组件化、单一职责、分层设计
- 参考项目中其他代码的设计，优先复用已有组件

## TypeScript 规范

- 严格禁止使用 `any` 类型，使用 `unknown` 替代
- 所有函数参数和返回值必须有明确类型声明
- 优先使用 `@shared/types` 中的共享类型定义

### 箭头函数规范

**禁止省略函数体和 return 语句**

```typescript
// ❌ 错误
export const getUserInfo = () => get<UserInfo>('/api/users/me');

// ✅ 正确
export const getUserInfo = async (): Promise<UserInfo> => {
  return get<UserInfo>('/api/users/me');
};
```

## React 组件规范

- 使用函数式组件 + `FC<Props>` 类型
- 统一使用 `export const Xxx: FC<Props>` 定义组件
- 禁止使用 `export default function` 定义组件
- 复杂渲染逻辑拆分成子组件，逻辑拆分为自定义 hook

## useCallback/useMemo 使用规范

**核心原则：非必要不使用，保持代码简洁**

### 需要使用 useCallback 的情况

1. 自定义 Hook 返回的函数
2. 传递给 `React.memo` 包裹的子组件的回调
3. 作为 useEffect 依赖项的函数

### 不需要使用的情况

- 组件内普通事件处理函数不需要 useCallback

## 状态管理规范（Zustand Store）

### 组件中使用 Store

**必须使用选择器模式，禁止直接解构**

```typescript
// ❌ 错误
const { documents, isLoading } = useDocumentStore();

// ✅ 正确
const documents = useDocumentStore((state) => state.documents);
const isLoading = useDocumentStore((state) => state.isLoading);
```

### 组件中获取 Actions

**必须通过选择器获取，禁止使用 `getState()`**

```typescript
// ❌ 错误：在组件中使用 getState()
const { fetchDocuments, deleteDocument } = useDocumentStore.getState();

// ✅ 正确：通过选择器获取
const fetchDocuments = useDocumentStore((state) => state.fetchDocuments);
const deleteDocument = useDocumentStore((state) => state.deleteDocument);
```

### Store 禁止事项

- ❌ State 中使用 `any` 类型
- ❌ 直接修改 state（必须用 `set` 或 `produce`）
- ❌ `useXxxStore()` 不传参数直接调用
- ❌ `get().xxx` 链式访问
- ❌ 在函数中间调用 `xxxStore.getState()`
- ❌ 在组件中使用 `useXxxStore.getState()` 获取 actions（必须用选择器）
- ❌ 选择器返回新创建的对象/数组作为默认值（会导致无限循环）

## App Router 规范

- 页面组件放在 `app/` 目录，使用路由组 `(group)` 组织
- 客户端组件必须添加 `'use client'` 指令
- 服务端组件优先，仅在必要时使用客户端组件
- 编辑器等交互组件使用 `dynamic` 禁用 SSR

## 命名规范

| 类型       | 规范             | 示例                 |
| ---------- | ---------------- | -------------------- |
| 组件名     | PascalCase       | `DocumentEditor`     |
| 文件/目录  | kebab-case       | `document-editor`    |
| 变量/函数  | camelCase        | `onSelectBlock`      |
| 常量       | UPPER_SNAKE_CASE | `DEFAULT_BLOCK_TYPE` |
| Store Hook | `use[Name]Store` | `useDocumentStore`   |

## 代码质量要求

- 添加必要的注释、类型定义、错误处理
- 不要在 JSX 中写复杂逻辑（提取成函数）
- 使用 `React.memo` 优化重渲染
- 大型列表使用虚拟滚动

## JSX 条件渲染规范

**禁止使用三目运算符进行条件渲染，使用 `&&` 逻辑运算符**

```tsx
{
  isLoading && <LoadingSpinner />;
}
{
  !isLoading && hasError && <ErrorMessage />;
}
```

**多条件样式/文本：使用映射对象代替嵌套三目**

```tsx
const TYPE_STYLES: Record<Type, string> = {
  user: 'bg-blue-500',
  error: 'bg-red-500',
  default: 'bg-white',
};

<div className={cn('base', TYPE_STYLES[type])} />;
```
