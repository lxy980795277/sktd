---
description: Tiptap 组件 UI 规范
alwaysApply: true
---

# Tiptap 组件 UI 规范

## 核心原则

**所有 `components/tiptap/` 目录下的组件必须严格遵循统一的 UI 风格**

## UI 组件使用规范

- 必须使用 Tiptap 内置 Button/Tooltip/Badge
- 图标必须使用 `tiptap-button-icon` 类名
- 使用 `data-size`、`data-style` 等属性控制样式

```typescript
import { Button } from '@/components/tiptap/ui-primitive/button';

<Button tooltip="运行">
  <Play className="tiptap-button-icon" />
</Button>
```

## 样式规范

- ❌ 禁止 `&` 拼接类名
- ❌ 禁止直接覆盖 `.tiptap-button-icon` 颜色（用 CSS 变量）

```scss
// ❌ 错误
.sql-block { &-header { ... } }

// ✅ 正确
.sql-block-header { ... }
```

## SCSS 文件命名

- Tiptap 组件样式文件必须使用 `组件名.scss`
- ❌ 禁止使用 `index.scss`

## 禁止事项

- ❌ 禁止在 tiptap 目录下使用 `@/components/ui/` 的组件
- ❌ 禁止使用 Shadcn UI 组件
- ❌ 禁止使用 lucide-react 的 `size` 属性
