---
description: Magic Book 前端样式开发规范
alwaysApply: true
---

# 前端样式开发规范

## UI 组件库规范

### Shadcn UI

**核心原则：除 Tiptap 编辑器外，所有 UI 组件优先使用 Shadcn UI**

- ✅ 使用 Shadcn UI 组件
- ✅ Tiptap 组件可使用 Radix UI（限 `@/components/tiptap/`）
- ✅ 可以引入其他 UI 组件库，但必须先征得用户同意
- ❌ 禁止在 Tiptap 之外直接使用 Radix UI

```typescript
import { Button } from '@/components/ui/button';

<Button variant="outline" size="sm">保存文档</Button>
```

### 图标库（lucide-react）

- ✅ 统一使用 `lucide-react`
- ❌ 禁止使用其他图标库

## 样式技术选择

1. 优先使用 TailwindCSS
2. 复杂样式使用 SCSS
3. 自定义样式使用 CSS 变量
4. 禁止内联样式

## CSS 变量规范

- 禁止硬编码颜色值
- 禁止使用 CSS 变量默认值（fallback）

```scss
// ❌ 错误：使用默认颜色值
color: var(--status-success, #52c41a);

// ✅ 正确
color: var(--status-success);
```

## SCSS 规范

- 使用 SCSS 语法（`.scss`）
- 禁止使用 `&` 拼接类名
- 使用 BEM
- 嵌套不超过 3 层

```scss
// ❌ 错误
.document-editor {
  &-toolbar { ... }
}

// ✅ 正确
.document-editor-toolbar { ... }
```

## 响应式设计规范

- 仅桌面/平板基础适配
- 不做移动端专项适配

## 导入顺序（Prettier 强制）

- 样式导入放最后
- 遵循项目既定顺序

## 性能优化

- 动画使用 `transform/opacity`
- 避免复杂选择器
