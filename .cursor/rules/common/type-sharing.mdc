# 前后端类型共用规范

## 核心原则

**单一事实来源 (Single Source of Truth)** - 前后端共用同一套类型定义，确保数据结构的一致性。

## 强制规范

### 1. 类型定义位置

- ✅ **必须**：所有共用类型放在 `packages/shared-types/` 目录
- ✅ **必须**：按功能模块拆分文件（如 `document.types.ts`, `user.types.ts`）
- ❌ **禁止**：在前端或后端项目中单独定义 API 相关类型

### 2. 共用内容规范

#### 应该共用的类型
- ✅ **DTO (Data Transfer Object)**：API 请求/响应的数据结构
- ✅ **业务模型**：前后端都需要的业务逻辑类型
- ✅ **枚举类型**：状态码、常量等
- ✅ **接口规范**：API 路径、方法等

#### 禁止共用的类型
- ❌ **数据库实体 (Entity)**：包含敏感字段（password, deleted_at 等）
- ❌ **内部实现**：后端特有的服务层、中间件类型
- ❌ **环境配置**：数据库连接、密钥等

### 3. 命名规范

```typescript
// ✅ 正确：DTO 命名
export interface CreateDocumentDto { ... }
export interface DocumentResponseDto { ... }
export interface UpdateUserDto { ... }

// ✅ 正确：业务模型命名
export interface Document { ... }
export interface User { ... }

// ❌ 错误：不要共用 Entity
export interface DocumentEntity { ... }  // 禁止
export interface UserModel { ... }       // 禁止
```

### 4. 文件结构规范

```
packages/shared-types/src/
├── index.ts              # 统一导出
├── document.types.ts     # 文档相关类型
├── user.types.ts         # 用户相关类型
├── api.types.ts          # API 通用类型
└── common.types.ts       # 通用基础类型
```

### 5. 导入使用规范

```typescript
// ✅ 前端使用
import type { Document, CreateDocumentDto } from '@shared/types';

// ✅ 后端使用
import { Document, CreateDocumentDto } from '@shared/types';

// ❌ 禁止从其他路径导入
import { Document } from '../../../shared/types';  // 禁止
```

## 开发流程

### 新增类型定义
1. 在 `packages/shared-types/src/` 中创建或修改类型文件
2. 在 `index.ts` 中导出新类型
3. 前后端项目通过 `@shared/types` 导入使用

### 修改现有类型
1. 修改 `packages/shared-types/` 中的类型定义
2. 运行 TypeScript 检查，修复所有报错
3. 确保前后端都能正常编译

### 版本控制
- 类型定义的修改必须向后兼容，或者同时更新前后端代码
- 破坏性变更需要通过版本号管理

## 错误处理

### 常见错误
1. **直接共用 Entity**：暴露敏感字段
2. **类型不同步**：前后端使用不同的类型定义
3. **循环依赖**：类型文件之间的循环引用

### 解决方案
1. 使用 DTO 模式，明确区分内部模型和外部接口
2. 建立 CI 检查，确保类型定义的一致性
3. 合理拆分类型文件，避免循环依赖

## 工具支持

### Monorepo 场景（推荐）
- 使用 TypeScript Project References
- 配置 `tsconfig.json` 的 `paths` 映射
- 利用 IDE 的类型检查和自动补全

### 非 Monorepo 场景
- 使用 `openapi-typescript` 生成类型
- 通过 npm 包发布共享类型
- 建立自动化的类型同步流程

## 验收标准

- [ ] 所有 API 相关类型都在 `packages/shared-types/` 中定义
- [ ] 前后端项目中没有重复的类型定义
- [ ] 类型修改后前后端都能正常编译
- [ ] 没有直接共用数据库 Entity 类型
- [ ] 类型文件按功能模块合理拆分