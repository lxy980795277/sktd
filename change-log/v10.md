2026-04-15

chore(i18n): 路由仅开放 EN/DE 并补充 RouteLocale

## 改进优化

### i18n

- `locales` 仅导出 `["en","de"]`，`isLocale` 收窄为 `RouteLocale`，不再生成 `/zh` 路由
- 以类型别名保留完整 `Locale`（含 `zh`）以兼容各 `Record<Locale, …>` 文案表；`defaultLocale` 改为 `RouteLocale`

### 前端

- `LanguageSwitcher`、`SiteHeader` 的 `locale` 入参改为 `RouteLocale`，与 `locales.includes` 的类型一致

### 工程

- `src/i18n/content.ts` 按 Prettier 调整部分对象属性的换行排版，保证 `format:check` 通过
