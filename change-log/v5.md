2026-04-14

feat(web): 重构首页模块并新增轮播横幅体验

## 新增功能

### 前端

- 新增 `featured-banner` 组件，支持 Embla 轮播、自动播放与指示器交互
- 新增通用 `ui/carousel` 组件并引入 `embla-carousel-react` 依赖
- 新增 `public/images/carous/` 与 `public/images/goods/` 素材目录
- 首页新增 `home/index.tsx` 统一导出入口，路由页面改为从该入口引入
- 多语言内容新增 `featuredBanner` 字段（EN/DE/ZH）并接入首页展示

## 改进优化

### 前端

- 将首页关键组件改为目录化结构（`hero-carousel`、`site-header`、`language-switcher`、`select`）
- 重写 `hero-carousel` 展示样式与交互，优化首屏视觉层次与切换体验
- 优化 `site-header` 移动端抽屉导航与语言切换交互
- 新增全局滚动条样式，统一滚动视觉风格
- `layout` 增加 `suppressHydrationWarning`，`not-found` 样式改为统一设计变量写法
