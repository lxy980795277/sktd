2026-04-15

feat(web): 抽取 locale 共享布局并新增关于与产品模块

## 新增功能

### 前端

- 新增 `src/app/[locale]/layout.tsx`，在语言路由下统一挂载 `SiteHeader` 与 `SiteFooter`
- 新增关于页 `src/app/[locale]/about/page.tsx`、`src/i18n/about-content.ts` 及根路径 `/about` 重定向
- 新增产品列表与详情路由 `src/app/[locale]/products/`、`src/constants/products.ts` 与 `src/components/products/` 展示组件
- 新增 `/products`、`/produces` 及对应 `[locale]` 下 `produces` 兼容路径，重定向至正确产品路由
- 导航与首页文案补充「产品」「关于我们」等独立页面入口

## 改进优化

### 前端

- `HomePage` 移除重复的顶栏外 `SiteHeader`/`SiteFooter`，仅保留 `TopBar` 与内容区块装配
- 联系页、首页各 Section、产品相关组件与文档等样式与结构微调，并统一 Prettier 格式

### 工程

- 新增 `@types/nodemailer` 解决 `nodemailer` 的 TypeScript 声明缺失
- 修复 `Carousel` 中 `onSelect` 回调与 `CarouselPrevious`/`CarouselNext` 的 props 类型定义，消除 `tsc` 报错
