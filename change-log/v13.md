2026-04-23

feat(web): 重构关于页与产品展示并增强联系页布局

## 新增功能

### 前端

- 关于页改版：`StorySections` 滚动叙事、`MilestonesTimeline` 时间线、`MilestonesCarousel` 配图轮播、`GoodsGallery` 底部瀑布流，配套 `about.css` 与子模块样式
- 产品列表 `ProductsShowcase` 改为按分类纵向全量展示，使用 `IntersectionObserver` 触发网格入场动画，新增 `products-showcase.css`
- 联系页全宽背景图、渐变遮罩与左右分栏（左侧文案与联系信息、右侧表单卡片）

## 改进优化

### 前端

- 产品详情页标题区与版式与关于页风格对齐；`ProductGallery` 采用固定纵横比容器、圆角与缩略图导航布局调整
- `globals.css` 新增 `--line-strong` 分隔线色变量
- 首页暂时注释 `SectorsSection` 并移除未使用 import；`market-film-section` 样式微调
- `about-content` 各语言里程碑标题精简为更短展示用语
- `ContactForm` 增加 `autoComplete="off"`，敏感字段使用 `new-password` 以降低浏览器误填
- `content.ts` 等文案或配置小步同步
- 全仓库 Prettier 格式化上述新增与改动文件
