2026-04-15

feat(web,api): 新增联系表单与邮件接口并拆分首页模块

## 新增功能

### API

- 新增 `POST /api/contact`：基于 nodemailer 的邮件发送、字段校验、IP 限流、`company` 蜜罐字段

### 前端

- 新增多语言 `/[locale]/contact` 页面、`/contact` 重定向至 `/en/contact` 与 `ContactForm` 组件及 `contact-content` 文案
- 根布局集成 Sonner `Toaster`；新增 `ui/button`、`input`、`label`、`textarea`、`sonner` 基础组件
- 新增多块首页 Section 子组件（含 TopBar、SiteFooter、About、Advantages、CTA、Milestones、Sectors、Testimonials 等）及 Planning、Services 预留组件
- 引入 `nodemailer`、`sonner` 依赖并更新 lockfile
- 新增 `public/images/logo/`、`public/images/goods/7.jpg`、`public/images/goods/8.jpg` 等静态资源

### 文档

- 新增 `docs/网站介绍.md`

## 改进优化

### 前端

- 首页 `index.tsx` 改为装配入口，暂时注释 Planning、Services 区块引用
- 重构 `HeroCarousel` 布局与交互；`FeaturedBanner` 补充独立 `index.css`
- `SiteHeader` 使用 Logo 图片替换文字徽标；类名与样式细节微调
- 调整 `globals.css` 中 `container-shell` 最大宽度与断点内边距，链接样式不再强制继承文本色
- 同步调整首页 `content` 数据结构/文案；语言配置将 `zh` 标签由「中文」改为「ZH」
