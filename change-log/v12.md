2026-04-21

feat(web): 新增市场胶片展示条并精简顶栏

## 新增功能

### 前端

- 新增 `MarketFilmSection`：双行商品图「胶片」横向滚动展示，配套 `index.css` 动效与遮罩

## 改进优化

### 前端

- 首页在「优势」与「行业」区块之间插入 `MarketFilmSection`
- `SiteHeader` 移除 `appVersion` 与 `DEV v*` 徽章；`[locale]/layout` 不再读取 `package.json` 传入版本
- 顶栏内边距、导航字重/字距与 uppercase、CTA 按钮尺寸等视觉微调；移动端菜单按钮略放大
