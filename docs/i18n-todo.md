# 多语言后续 TODO

本轮开放 EN / DE / ES / IT，英文为翻译基准，德文为参考，中文保留但不开放。以下项目按本轮确认的范围暂缓。

- [ ] **HTML 语言声明**：让服务端输出的 `html lang` 跟随当前语言路由。本轮保留根布局中的 `lang="en"`。
- [ ] **联系接口错误本地化**：API 返回稳定错误码，由前端映射各语言提示。本轮保留现有接口返回消息，具体错误仍可能显示英文。
- [ ] **切换语言保留上下文**：保留查询参数和 hash 锚点。本轮继续沿用 pathname 切换，分类参数可能丢失并回到默认分类。
- [ ] **多语言 SEO 完善**：补齐 canonical、hreflang、跨语言页面对应关系和 sitemap 等。已有标题、description 文案随语言翻译，SEO 生成机制保持现状。
- [ ] **四语言布局适配及视觉专项验收**：后续检查长标题、导航、关于页窄屏、里程碑卡片、产品名称和字体显示。本轮保留既有布局、样式、动画及标题 `/` 分段方式。
